import { render, screen } from '@testing-library/react'
import { TestComponent.test } from './TestComponent.test'

describe('TestComponent.test', () => {
    it('renders correctly', () => {
        render(<TestComponent.test />)
        // Add assertions here
    })
})
