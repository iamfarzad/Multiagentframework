import { render, screen } from '@testing-library/react'
import { TestComponent.test.test } from './TestComponent.test.test'

describe('TestComponent.test.test', () => {
    it('renders correctly', () => {
        render(<TestComponent.test.test />)
        // Add assertions here
    })
})
